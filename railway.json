{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pip install -r dealflow_assistant/requirements.txt",
        "startCommand": "uvicorn dealflow_assistant.main:app --host 0.0.0.0 --port $PORT"
    }
}