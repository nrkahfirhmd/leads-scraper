{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd dealflow_assistant && pip install -r requirements.txt",
        "startCommand": "cd dealflow_assistant && uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
}